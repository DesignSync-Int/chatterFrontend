# NPM configuration for better dependency resolution
engine-strict=false
save-exact=false
audit-level=moderate

# Rollup binary configuration
# Try to download native binaries, but don't fail if unavailable
optional=true
